// TRIODE deploy script — placeholder for now.
// Real deployment logic will be added in a later step.
const hre = require("hardhat");

async function main() {
  console.log("Deploy script placeholder — TriodeMarket address will go here");
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
